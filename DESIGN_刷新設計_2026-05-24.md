# ファイアライフBASE アプリ デザイン刷新設計（2026-05-24 ウルトラシンク）

> 「初期に作って全体的にダサい」状態を、ブランド一貫・モダンに刷新する設計。実装はこの設計に沿って一気に進める。

## 1. ブランド/配色の決定
- **primary＝インディゴ `#4F46E5`**（hsl 244 75% 59%）。理由：マネコンの「with FIRE」ブランド色と統一＝全プロパティでブランド一貫。信頼感のあるfintech標準色。
- **brand（アクセント）＝フレイム・オレンジ `#F97316`**（hsl 24 94% 53%）。🔥ロゴ・FIREの熱・ハイライトに使う1点アクセント。
- **ニュートラル＝クールグレー**（純グレーでなく青み）。背景ほぼ白・前景スレート系。
- 旧primaryの緑は廃止（エンタープライズ感・ダサさの元）。
- レインボーなアイコン色（dashboard 7色バラバラ）→ **インディゴのタイルに統一**。
- budgetの「固定費=青／変動費=橙」は"意味のある色分け"なので維持。

## 2. タイポ階層（globals.css @layer base）
- h1 30px/700/tight・h2 24px/700・h3 20px/600・body line-height 1.65
- フォントはNoto Sans JP維持

## 3. 形状・質感
- `--radius` を 0.5rem → **0.75rem**（やわらかくモダンに）
- カード：rounded-xl・繊細なborder・soft shadow・hoverで軽くlift
- ボタン：indigo primary・hover明確・focus ringはindigo

## 4. 実装スコープ（優先度順）
1. **globals.css 刷新**（色・タイポ・radius・brand変数）＝全ページに波及・最大インパクト
2. **ヘッダー/ログインのFlame**をbrandオレンジに（indigo一色にせず熱を残す）
3. **ダッシュボード再設計**：インディゴ統一タイル・4STEP順（実施済）・ウェルカム・お知らせ刷新（Q&A言及削除）
4. **家計簿（6枠/summary）磨き込み**：余白・カード・サマリー
5. 他ページ（guide/dream/community/courses/live/assets）はglobals波及で底上げ＋順次磨く

## 5. Tailwind v4 メモ
- 設定はCSSベース（tailwind.configなし）。`@theme inline` に `--color-brand` を追加して `bg-brand/text-brand` を使えるように。
- ダークモードのCSS変数も同時更新（将来の dark: 対応に備える）。

## 6. 実装ルール
- 既存機能は壊さない（色・余白・タイポ中心の刷新）。
- 大量の不可逆削除はしない。
- ビルド（prisma generate && next build）で確認してからコミット候補に。
